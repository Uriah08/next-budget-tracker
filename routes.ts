
// Array of routes that accessible in public

// Routes that will redirect the user in home once logged in
export const authRoutes = [
    "/auth/login",
    "/auth/register"
];

export const apiAuthPrefix = "/api/route"

export const DEFAULT_LOGIN_REDIRECT = "/startup"